<%= form_for([@campus, @propuesta], :url => {:controller => :propuestas, :action => (@propuesta.new_record? ? :create : :update) }) do |f| %>
  <% if @propuesta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@propuesta.errors.count, "error") %> prohibited this propuesta from being saved:</h2>

      <ul>
      <% @propuesta.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br />
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_area :descripcion %>
  </div>
  <div class="field">
    <%= f.label :categoria %><br />
    <%= f.text_field :categoria %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
