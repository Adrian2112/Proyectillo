<p id="notice"><%= notice %></p>

<p>
  <b>Nombre:</b>
  <%= @universidad.nombre %>
</p>

<%= link_to 'Campus', universidad_campus_index_path(@universidad) if can? :read, @universidad %>
<%= link_to 'Edit', edit_universidad_path(@universidad) if can? :edit, @universidad %>
<%= link_to 'Back', universidades_path %>
