<div>
	<%= universidad.nombre %>

    <%= link_to 'Show', universidad_campus_index_path(universidad) if can? :read, universidad %>
	  <%= link_to 'Edit', edit_universidad_path(universidad) if can? :edit, universidad %>
    <%= link_to 'Destroy', universidad, :confirm => 'Are you sure?', :method => :delete if can? :destroy, universidad %>
</div>
