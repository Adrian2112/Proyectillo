<% if !@resultados.blank? %>
  $("#resultados_index").html("<%= escape_javascript(render @resultados)%>");
<% elsif !@resultados.nil? %>
  $("#resultados_index").html("No se encontró nada");
<% end %>

<%
	show_more = escape_javascript(render(:partial => 'shared/show_more', :object => @resultados,
            														:locals => { :url => 'pages/mas_resultados.js',
																											:parametros => { :curso_nombre => :curso_nombre,
																																				:campus_id => :campus_id,
																																				:profesor_nombre => :profesor_nombre,
																																				:page => :page }
																																			}
																				)
																)

%>

$("#next_link").html("<%= show_more %>");
